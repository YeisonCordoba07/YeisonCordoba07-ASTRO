---
const {tagName, src} = Astro.props;
---

<div class="tag" >
    <img src={src} alt="tag icon"/>

    <span class="tag__name" >{tagName}</span>

</div>


<style>
    .tag {
        display: flex;
        justify-content: center;
        width: fit-content;
        align-items: center;
        gap: 10px;
        padding: 5px 10px;
        border-radius: 20px;
        border: 1px solid black;
        background-color: #f3f3f3;
    }

    .tag__name {
        font-size: 14px;
        font-family: fredoka, nunito, serif;
        font-weight: 500;
        color: #2f2f2f;
    }
</style>
